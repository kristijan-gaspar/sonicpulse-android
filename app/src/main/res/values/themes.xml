<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- AppCompat-derived parent required by AppCompatActivity (Settings §3 — per-app language
         support); Compose/Material3 owns the actual visual theme via SonicPulseTheme. -->
    <style name="Theme.SonicPulse" parent="Theme.AppCompat.DayNight.NoActionBar" />
</resources>
